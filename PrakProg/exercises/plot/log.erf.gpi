 \
	set terminal svg; \
	set output "erf.svg"; \
	set key left ;\
	set xlabel "x";\
	set ylabel "erf(x)";\
	plot  \
		"data.txt" index 0 with lines title "erf(x)" \
		,"erf.data" with points pointtype 4 title "tabulated erf(x)" \


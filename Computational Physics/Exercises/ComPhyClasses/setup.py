from setuptools import setup, find_packages

setup(
    name='ComPhyClasses',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    # Add other metadata here, like author, description, etc.
)